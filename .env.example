# signaldesk engine secrets — copy to .env (gitignored) and fill in. Never commit.
# Reddit OAuth app (type "script") — created at https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USERNAME=
# (password only if the script-app flow needs it; leave blank otherwise)
REDDIT_PASSWORD=
