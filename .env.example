# Local configuration — copy to .env (gitignored) and fill in only what you use.
# Never commit this file with values.
# Legacy Gemini brief generator
GEMINI_API_KEY=
# Optional legacy runtime overrides
DAYS_BACK=
OUTPUT_DIR=
# Optional deterministic engine configuration path
ENGINE_CONFIG=
REDDIT_USERNAME=
# (password only if the script-app flow needs it; leave blank otherwise)
REDDIT_PASSWORD=
